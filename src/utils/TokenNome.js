// var token = window.localStorage.getItem("session-token");
// if (token === null) {
//   parsedToken = "Desconhecido";
// }
// var parsedToken = JSON.parse(atob(token.split(".")[1])).Nome;

var parsedToken = "Desconhecido";

export default parsedToken;
